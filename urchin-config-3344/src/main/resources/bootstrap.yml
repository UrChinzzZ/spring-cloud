spring:
  application:
    name: config-single-server  # 应用名称
  cloud:
    config:
      server:
        git:
          uri: https://github.com/UrChinzzZ/spring-cloud-config #配置文件所在仓库
          username: urchin
          password: Tt19971220
          default-label: main #配置文件分支
          search-paths: config  #配置文件所在根目录
